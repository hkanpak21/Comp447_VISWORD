# CLIP ViT-B/16 frozen + Bottleneck adapter (768 → 64 → 256)
experiment_name: adapter-clip-bottleneck
model_kind: frozen_adapter

backbone:
  arch: clip_vitb16
  feature_dim: 768
  num_trainable_blocks: 0

adapter:
  kind: bottleneck
  output_dim: 256

data:
  num_train_samples: 10000
  num_eval_samples: 1000

train:
  loss: multisim
  batch_size: 16
  epochs: 3
  lr_backbone: 0.0
  lr_head: 5.0e-4
