# Row 16 — BERT-base zero-shot title-only text retrieval.
# Used by eval_text.py (text encoder is loaded directly; model_kind here
# is a placeholder for config parsing; we reuse the zeroshot Literal).
experiment_name: visword-row16-bert-title
model_kind: zeroshot_dinov2_cls   # placeholder; eval_text overrides

data:
  num_train_samples: 0
  num_eval_samples: 0

eval:
  k_values: [1, 5, 10, 20]
  phase1_max_pages: 0
  phase2_max_queries: 200
