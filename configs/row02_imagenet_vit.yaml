# Row 2 — ImageNet-supervised ViT-B/16 zero-shot.
experiment_name: visword-row02-imagenet-vit
model_kind: zeroshot_imagenet_vit

data:
  num_train_samples: 0
  num_eval_samples: 1000

eval:
  k_values: [1, 5, 10, 20]
  phase1_max_pages: 1000
  phase2_max_queries: 200
